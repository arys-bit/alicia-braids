{
  "name": "alicia-braids-landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:sites": "node --test tests/sites-worker.test.mjs",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist/client"
  },
  "dependencies": {
    "@phosphor-icons/web": "2.1.2",
    "vite": "7.2.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
